[package]
name = "passport-core"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "Host-testable PassportOS logic: ADC decoder, compositor, launcher, app contract"
publish = false

[dependencies]
heapless.workspace = true

[lib]
name = "passport_core"
path = "src/lib.rs"
